<Window x:Class="ProductivityTools.MoveDisplay.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProductivityTools.MoveDisplay.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="66" Width="231">
    <Window.Resources>
        <ResourceDictionary>
            <BitmapImage x:Key="StopImage" UriSource="Resources\arrowsGreen.png"/>
            <BitmapImage x:Key="PlayImage" UriSource="Resources\arrowsGrey.png"/>
        </ResourceDictionary>
    </Window.Resources>
    <!--<ThumbButtonInfoCollection>
        <ThumbButtonInfo
                    DismissWhenClicked="False"
                    Command="MediaCommands.Play"
                    CommandTarget="{Binding ElementName=btnPlay}"
                    Description="Play"
                    ImageSource="{StaticResource ResourceKey=PlayImage}"/>
        <ThumbButtonInfo
                    DismissWhenClicked="True"
                    Command="MediaCommands.Stop"
                    CommandTarget="{Binding ElementName=btnStop}"
                    Description="Stop"
                    ImageSource="{StaticResource ResourceKey=StopImage}"/>
    </ThumbButtonInfoCollection>-->
    <!--<TaskbarItemInfo x:Name="taskBarItemInfo1" 
                     Overlay="{StaticResource ResourceKey=PlayImage}"
                     ThumbnailClipMargin="80,0,80,140"
                     Description="Taskbar Item Info Sample">-->
        <Window.TaskbarItemInfo>
        <TaskbarItemInfo x:Name="taskBarItemInfo1">
            <TaskbarItemInfo.ThumbButtonInfos>
                <ThumbButtonInfoCollection>
                    <ThumbButtonInfo

                    Description="Play"
                    ImageSource="{StaticResource ResourceKey=PlayImage}"/>
                    <ThumbButtonInfo
                  
                    Description="Stop"
                    ImageSource="{StaticResource ResourceKey=StopImage}"/>
                </ThumbButtonInfoCollection>
            </TaskbarItemInfo.ThumbButtonInfos>
        </TaskbarItemInfo>
    </Window.TaskbarItemInfo>
    <Grid Margin="0,0,0,4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0*"/>
            <ColumnDefinition Width="0*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="MoveLeft" Content="Move display to left" HorizontalAlignment="Left" VerticalAlignment="Center" Height="20" Width="109" Click="MoveLeft_Click"/>
        <Button x:Name="MoveRight" Content="Move display to right" HorizontalAlignment="Left" Margin="110,0,0,0" VerticalAlignment="Center" Height="20" Width="117" Click="MoveRight_Click"/>

    </Grid>
</Window>
